#include<stdio.h>

int main(){

    // char name[100];
    // // gets(name);
    // fgets(name,100,stdin);
    // puts(name);

    char *change = "Hello world ";
    puts(change);
    change = "Hello";
    puts(change);

    // char cannotChange[] = "hello world";
    // puts(cannotChange);
    // cannotChange = "hello";


    return 0;
}